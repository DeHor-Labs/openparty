# URL base do servidor OpenParty (HTTP e WS).
# Em desenvolvimento local com proxy Vite, esta variavel pode ser omitida.
# Em producao (Docker, Vercel, etc.), defina com a URL real do servidor.
VITE_SERVER_URL=http://localhost:3000
